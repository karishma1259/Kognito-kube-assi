import React from 'react'

export default function MenuCard({ title, desc, price, img }) {
  return (
    <article className="bg-white shadow-md rounded-xl overflow-hidden hover:shadow-xl hover:-translate-y-1 transition transform animate-pop">
      <img src={img} alt={title} className="w-full h-48 object-cover" />
      <div className="p-4 flex flex-col justify-between">
        <h3 className="text-lg font-semibold">{title}</h3>
        <p className="text-gray-500 text-sm">{desc}</p>
        <div className="mt-4 flex justify-between items-center">
          <span className="font-bold">{price}</span>
          <button className="border px-3 py-1 rounded hover:bg-orange-50 transition">Add</button>
        </div>
      </div>
    </article>
  )
}
